# Quick Start

```bash
git clone https://github.com/lancemorii-git/gate.git
cd gate
cargo test --workspace
cargo run -p gate-server
```

Start the desktop client:

```bash
cd client
npm install
npm run tauri dev
```

Continue with [Install](./install.md) and [Configuration](./configuration.md).
