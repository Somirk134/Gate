# Gate Screenshot Assets

All screenshot previews use the same public presentation style:

- Canvas: `1440x900`.
- Background: `#eef3f8`.
- App frame radius: `28`.
- Inner panel radius: `14`.
- Shadow: soft neutral shadow.
- Typography: Inter, Segoe UI, Arial fallback.
- Primary color: `#0ea5e9`.
- Status color: `#10b981`.

These SVGs are documentation previews for the alpha project. Replace them with real captured screenshots before a stable public release while keeping the same dimensions and framing.
